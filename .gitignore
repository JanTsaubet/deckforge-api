# Dependencias y compilación
node_modules
dist
# El worker compila aparte para no borrarle `dist` a la API en modo --watch.
dist-worker
coverage
*.tsbuildinfo

# Variables de entorno (solo se versiona .env.example)
.env
.env.*
!.env.example

# Sistema y logs
.DS_Store
Thumbs.db
npm-debug.log*
